name: TalkTime Daily

on:
  schedule:
    - cron: "0 22 * * *" # UTC 22:00 = KST 07:00 (예: UTC 22:00 = KST 07:00)
  workflow_dispatch:
  push: # input/ 저장(커밋) 시 즉시 생성+배포(후킹), 사이트 파일 변경 시 배포
    branches: [main]
    paths:
      - "input/**"
      - "content/**"
      - "hugo.toml"
      - "archetypes/**"
      - "assets/**"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: talktime-daily
  cancel-in-progress: false

jobs:
  generate:
    # push 이벤트에서도 실행 — input/script.md 저장이 곧 후킹 트리거.
    # 이미 게시된 스크립트는 해시 dedup으로 즉시 스킵되므로 중복 실행 비용 없음.
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.commit.outputs.changed }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pipeline/requirements.txt

      - name: Install dependencies
        run: pip install -r pipeline/requirements.txt

      - name: Install Claude Code CLI
        run: npm install -g @anthropic-ai/claude-code

      - name: Generate today's post
        id: generate
        continue-on-error: true
        env:
          # CLAUDE_CODE_OAUTH_TOKEN 등록 시 구독 인증(claude-code), 없으면 API 키 과금(api)
          JUDGE_BACKEND: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN != '' && 'claude-code' || 'api' }}
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python pipeline/generate.py

      - name: Commit new post
        id: commit
        run: |
          git config user.name "diary-bot"
          git config user.email "actions@users.noreply.github.com"
          git add content/posts pipeline/state.json
          if git diff --cached --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "신규 포스트 없음 — 커밋 스킵"
          else
            git commit -m "chore: daily post $(date -u +%F)"
            git push
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Surface generation failure
        if: steps.generate.outcome == 'failure'
        run: |
          echo "::error::생성 단계 실패 (CLAUDE_CODE_OAUTH_TOKEN/크레딧 확인 필요)"
          exit 1

  deploy:
    needs: generate
    # 스케줄: 신규 포스트 있을 때만 / 수동·push: 항상 배포
    if: ${{ !cancelled() && (needs.generate.outputs.changed == 'true' || github.event_name == 'workflow_dispatch' || github.event_name == 'push') }}
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          ref: main
          fetch-depth: 0

      - uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: "0.161.1"
          extended: true

      - uses: actions/configure-pages@v5
        id: pages

      - name: Build site
        run: hugo --minify --baseURL "${{ steps.pages.outputs.base_url }}/"

      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

      - id: deployment
        uses: actions/deploy-pages@v4
